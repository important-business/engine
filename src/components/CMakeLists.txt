# Define sources
set(SRCS_COMPONENTS '')

# Define library
# TODO: Enable once source files are added
# add_library(components STATIC ${SRCS_COMPONENTS})

# Add Anax to include directories
# target_include_directories(components PUBLIC ${ANAX_INCLUDE_DIR})

